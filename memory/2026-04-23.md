Editorial batch completed for seven judicial opinions.

- Added separate editorial overlay records for:
  - `tavarez-v-state-319-ga-480`
  - `golden-v-floyd-healthcare-management-inc-319-ga-496`
  - `grant-v-state-319-ga-490`
  - `heyward-v-state-319-ga-588`
  - `shahid-v-esaam-376-ga-app-145`
  - `profet-v-state-322-ga-731`
  - `woods-v-state-322-ga-365`
- Kept raw opinions untouched in `src/data/cases.json`.
- Regenerated `src/data/case-editorial.json` report with `npm run sync-editorial`.
- Confirmed the app still builds cleanly with `npm run build`.
- Added the remaining missing editorial overlays from the report, including alias-matched rows for `Arnsdorff`, `Bailey`, `Barker`, `Richardson`, `State v. Phillips`, and `Woods`.
- The latest sync shows zero remaining `missing-tags, missing-summary` rows in `tmp/case-editorial-report.md`.
- Expanded the tarot reader dialogue system in `public/tarot/dialogue.js` with much larger opening, intent, spread, draw, reveal, reading, and closing text banks.
- Kept the tarot UI/stage flow untouched and left the oracle game completely independent.
- Added bridge dialogue helpers and wired the tarot game to use them for intent, spread, reading lead-ins, and closing prompts.
- Adjusted the psychic sprite in `public/game-model.html` by centering the aura behind the figure and re-centering the jewel row on the torso.
- Audited the case-law pages for integrity concerns.
  - Confirmed two high-priority text-loss cases from the saved audit: `fleureme-v-city-of-atlanta-2026-ga-app-lexis-37` and `mcelrath-v-georgia`.
  - Found several suspected truncations that still need human review: `lee-v-state`, `perez-v-bush-2025-ga-state-lexis-860`, `roman-v-state`, `ga-cvs-pharm-llc-v-carmichael-316-ga-718`, `myrick-v-fulton-cnty-69-f-4th-1277`, and `inquiry-cerning-judge-coomer-315-ga-841`.
  - Footnote handling is not fully normalized: some opinions still carry raw nested superscript markup, and some inline note links use `#fnN` anchors that do not match the slug-prefixed footnote IDs rendered by `CaseLawBox`.
- Follow-up integrity pass showed the older length-based audit was a false alarm for the canonical published corpus.
  - `Fleureme v. City of Atlanta` is genuinely short because the post-processed published opinion is only the body text, while the raw DOCX length includes metadata and front matter.
  - Added parser normalization in `scripts/process-cases.js` so nested superscript footnote artifacts and bare `#fnN` anchors are converted into canonical `{fn:N}` markers before publication.
  - Rewrote `src/data/cases.json` to apply that normalization to existing opinion text entries.
